# 开 PR / 推分支前的最终真实启动冒烟(2026-09-04 v0.4.1 事故:两处"presubmit
# 全绿但所有用户启动即崩"的回归)。全部门禁静态层面都在 pre-commit 跑过,
# 这一句额外验证"主干构建产物在用户等价的解析池里真能 boot 起来"。
# 跳过(仅限你清楚改动不可能影响启动时):git push --no-verify
pnpm smoke:profile-boot
